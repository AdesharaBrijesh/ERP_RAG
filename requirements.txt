# --- Web service ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- Database ---
SQLAlchemy>=2.0
psycopg[binary]>=3.2
sqlparse>=0.5

# --- Retrieval ---
numpy>=1.26

# --- LLM providers ---
groq>=0.13          # local dev / staging
boto3>=1.35         # AWS Bedrock (production target)

# --- Session store (optional backend) ---
redis>=5.2

# --- Tests / eval ---
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
PyYAML>=6.0

# --- Optional local embeddings (ONNX, no torch). Install with:
#     pip install fastembed
# Falls back to the built-in hashing embedder if absent.
